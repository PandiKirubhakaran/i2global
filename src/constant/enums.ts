export enum AuthLable {
  Login = "Login",
  Register = "Register",
  SignUp = "Sign Up",
  Homepage = "Homepage",
}

export enum NavBarLable {
  KeepNotes = "KeepNotes",
  About = "About",
  Notes = "Notes",
  Account = "Account",
  Login = "Login",
}

export enum ButtonLable {
  Add = "Add",
  Cancel = "Cancel",
  Delete = "Delete",
  Edit = "Edit",
  LastModifier = "Last Modifier:",
}
